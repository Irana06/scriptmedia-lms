<div class="space-y-6">
    <div class="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><x-theme.section-header eyebrow="Hasil belajarmu" title="Nilai & presensi" description="Rekap hasil akhir dan kehadiran setiap semester." /><label class="min-w-64 text-sm font-semibold text-navy">Semester<select wire:model.live="semesterId" class="mt-2 min-h-11 w-full rounded-xl border border-line bg-white px-3">@foreach($semesters as $item)<option value="{{ $item->id }}">{{ $item->academicYear->year_label }} · {{ $item->name }}</option>@endforeach</select></label></div>
    <div class="grid grid-cols-2 gap-3 sm:grid-cols-4">@foreach(['hadir' => ['Hadir','tosca'], 'izin' => ['Izin','orange'], 'sakit' => ['Sakit','navy'], 'alpa' => ['Alpa','neutral']] as $status => [$label,$tone])<x-theme.card class="text-center"><p class="text-3xl font-semibold text-navy">{{ $attendanceCounts[$status] }}</p><x-theme.badge :tone="$tone" class="mt-2">{{ $label }}</x-theme.badge></x-theme.card>@endforeach</div>
    <x-theme.card :padding="false" class="overflow-hidden"><div class="border-b border-line px-5 py-5 sm:px-6"><h2 class="text-xl">Nilai akhir mata pelajaran</h2><p class="mt-1 text-sm text-ink-soft">{{ $semester ? $semester->academicYear->year_label.' · Semester '.$semester->name : 'Belum ada semester' }}</p></div>@if($grades->isEmpty())<div class="p-12 text-center text-sm text-ink-soft">Nilai akhir belum diterbitkan.</div>@else<div class="overflow-x-auto"><table class="min-w-full text-left text-sm"><thead class="bg-offwhite text-ink-soft"><tr><th class="px-5 py-3 sm:px-6">Mata pelajaran</th><th class="px-5 py-3">Guru</th><th class="px-5 py-3">Nilai</th><th class="px-5 py-3">Predikat</th></tr></thead><tbody class="divide-y divide-line">@foreach($grades as $grade)<tr><td class="px-5 py-4 font-semibold text-navy sm:px-6">{{ $grade->classSubject->subject->name }}</td><td class="px-5 py-4 text-ink-soft">{{ $grade->classSubject->teacher->name }}</td><td class="px-5 py-4 text-lg font-semibold text-navy">{{ number_format((float)$grade->final_score, 2) }}</td><td class="px-5 py-4"><x-theme.badge tone="tosca">{{ $grade->predikat }}</x-theme.badge></td></tr>@endforeach</tbody></table></div>@endif</x-theme.card>
</div>
